import { HydrateClient } from "~/trpc/server";

export default async function Home() {
  auth

  return (
    <HydrateClient>
      <div>
        <button onClick={() => {

        }}>Sign Up</button>
      </div>
    </HydrateClient>
  );
}
